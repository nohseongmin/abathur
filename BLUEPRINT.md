# 개조식(gaejosik) 블루프린트

## 한 줄 정의

한국어로 AI 코딩 에이전트를 쓰는 개발자가, **출력 토큰을 측정된 70%가량 줄이면서** 기술적 정확도는 그대로 유지하도록 만드는 Claude Code 스킬 + 검증 CLI.

## 가정

사용자에게 묻지 않고 내린 결정들. 뒤집고 싶으면 말만 하면 됨.

1. **이름은 `개조식 / gaejosik`.** 개조식은 한국 공문서·군 보고서의 실제 문체 표준(명사형 종결, 항목 나열, 군말 없음)이라 한국인 개발자가 설명 없이 즉시 이해함. caveman이 영어권 밈을 쓴 것과 같은 자리.
2. **1차 형태는 "라이브러리"가 아니라 "스킬 + 측정 CLI".** 출력 토큰은 모델이 *생성하기 전에* 줄여야 절약됨. 생성된 뒤 후처리로 압축하면 이미 과금은 끝났고 오히려 압축용 LLM 호출로 토큰이 추가됨. 따라서 절감의 본체는 스킬(생성 시점 스타일 지시)이고, 파이썬 패키지는 그 스킬이 진짜 효과가 있는지 **측정·회귀검증**하는 역할.
3. **토크나이저 기준은 tiktoken `o200k_base`(기본) / `cl100k_base`(대조).** Claude의 토크나이저는 비공개라 정확한 재현이 불가능. 대신 Anthropic `count_tokens` API 경로를 옵션으로 넣어, 키가 있으면 실제 Claude 수치를 측정 가능(`--anthropic`). 프록시라는 사실은 README에 명시.
4. **한국어 출력을 영어로 바꿔 절약하는 방식은 채택하지 않음.** 실측 1.4~2.3배 차이라 유혹적이지만, 한국어로 읽겠다는 사용자 요구 자체를 없애는 건 해결이 아니라 회피.
5. **배포는 GitHub 공개 레포 + Claude Code 플러그인 마켓플레이스 형식.** caveman과 동일한 설치 경험(`/plugin marketplace add`).
6. 가격 정보(모델별 $/Mtok)는 코드에 박지 않음 — 자주 바뀌고 내가 틀릴 수 있음. 비용 환산은 `--price-per-mtok` 인자로 사용자가 넣음.

## 시장성 & 경쟁

**문제는 실재하고 측정됨.** 한국어는 같은 의미를 표현할 때 영어보다 토큰을 더 씀 — 이 프로젝트 자체 실측으로 **1.38~2.29배**(o200k_base), 외부 보고는 1.5~3배, 대표값 2.36배. 출력 토큰은 입력보다 단가가 비싸므로(주요 API 통상 3~5배), 한국어 사용자는 같은 작업에 구조적으로 더 많은 돈·더 빠른 사용량 한도 소진을 감수함. Anthropic claude-code 레포에도 비영어권 사용자의 토큰 불이익에 대한 이슈(#26401)가 올라와 있음.

| 유사 서비스 | 하는 일 | 약점 |
|---|---|---|
| **caveman** (플러그인) | 영어 caveman 문체로 출력 65% 절감(자체 측정) | "사용자 언어 유지" 규칙은 있으나 규칙 자체가 영어 BPE 기준. 한국어에서 무엇이 실제로 토큰을 줄이는지는 검증되지 않음 |
| **LLMLingua** (MS 연구) | 프롬프트(입력) 압축 | 입력 전용. 출력 토큰은 대상 아님. 별도 압축 모델 필요 |
| **Claude `/compact`, `/effort`** | 컨텍스트 요약, 사고 예산 조절 | 범용. 한국어 특성과 무관 |
| 한국어 블로그의 "토큰 절약 N가지" | 팁 나열 | **측정이 없음.** 효과 없는 팁과 있는 팁이 섞여 있음 |
| 한국 마켓플레이스(모두의AI, Synapse) | 도메인 업무 스킬 | 토큰 절감 자체를 다루지 않음 |

**직접 경쟁자 부재.** 한국어 출력 토큰 절감을 정면으로 다루면서 수치로 검증하는 도구는 검색되지 않음.

**차별점 — 이게 이 프로젝트의 전부:**
caveman 문서에도 나오는 함정이 있음. "configuration → cfg" 같은 그럴듯한 축약은 토크나이저에서 **토큰을 하나도 못 줄임**. 한국어에도 똑같은 함정이 있고, 심지어 **한국인이 가장 먼저 떠올리는 축약이 바로 그 함정**임:

> `~합니다` → `~함` 으로 바꾸면 글자는 15% 줄지만 **o200k_base 기준 토큰은 0% 줄어든다** (10토큰 → 10토큰).
> `→` 화살표는 오히려 **토큰이 늘어난다** (3 → 4). `ㅇㅋ` 같은 자모 축약도 **이득 0**.

즉 "한국어 caveman"을 감으로 만들면 가독성만 버리고 돈은 그대로 나감. 실제 이득은 **삭제(인사·헤지·재진술·공손체)와 개조식 구조화**에서 나오고, 그 비중이 절감의 대부분임. 이 프로젝트는 모든 규칙을 토크나이저로 재서 근거가 있는 것만 싣고, **규칙이 효과 없어지면 테스트가 깨지게** 만든다.

## BM 매칭

**선택: 무료 OSS 성장 우선 → 2차 B2B 리포트.** (BM 가이드의 "커뮤니티/네트워크: 무료 성장 우선, BM은 규모 후"에 해당)

- **왜 지금 과금하지 않는가**: 개발자 도구 플러그인은 설치 마찰이 곧 죽음이고, 경쟁 상대인 caveman이 무료 OSS임. 유료 진입은 채택을 0으로 만듦.
- **왜 이 유저가 나중에 돈을 내는가**: 절감액이 곧 숫자로 보이는 도구임. 팀이 월 API/구독 비용 수백 달러를 쓰면, "우리 팀 한국어 출력 토큰 몇 % 절감, 얼마 아꼈다"는 **리포트**가 결재 가능한 상품이 됨.
- **언제부터 과금 가능한가**: GitHub 스타 500 또는 주간 설치 100 이상에서 팀용 유료 티어(사용량 집계 대시보드 + 조직 스타일 규칙 커스터마이즈). 그 전까지는 전액 무료.
- 2차 수익원: 같은 엔진의 일본어/중국어 확장(동일한 CJK 토큰 불이익 구조).

## 보안 체크리스트

이 아이디어가 실제로 다루는 데이터에 한정. 로그인·결제·개인정보 없음 → 해당 항목 없음.

- [x] **외부 API 키(선택적 Anthropic `count_tokens`)**: `ANTHROPIC_API_KEY`를 환경변수에서만 읽음. 소스·설정 파일에 키 하드코딩 금지, `.env`는 `.gitignore`.
- [x] **파일 입력 처리**: CLI가 임의 파일을 읽으므로 크기 상한을 두고, 인코딩 오류로 죽지 않게 처리. 경로는 `Path.resolve()`로 정규화.
- [x] **사용자 텍스트 외부 전송 차단**: 기본 동작(`bench`, `count`)은 사용자 텍스트를 전송하지 않음. 외부로 내보내는 경로는 `--anthropic`을 명시적으로 켤 때만 존재하고, README에 경고 명시. (tiktoken이 최초 1회 BPE 파일을 내려받는 것은 별개 — README에 사실대로 적음.)
- [x] **에러 메시지에 내부 정보 노출 금지**: 스택트레이스 대신 사람이 읽는 실패 사유.
- [x] **시크릿 하드코딩 금지 / 가격 등 변동값 하드코딩 금지**: 상수는 한 모듈에, 변동값은 인자로.

## 기술스택

| 항목 | 선택 | 근거 |
|---|---|---|
| 언어 | Python 3.10+ | 사용자 주력 언어. 토크나이저 생태계(tiktoken)가 파이썬 우선 |
| 토크나이저 | `tiktoken` | 유일하게 실무에서 쓰이는 오프라인 BPE 카운터. `transformers`는 이 용도에 과함 |
| CLI | `argparse` (표준 라이브러리) | typer/click 넣을 이유 없음. 서브커맨드 2개뿐 |
| 테스트 | `pytest` | 규칙 회귀 테스트가 이 프로젝트의 핵심 산출물 |
| 배포 | GitHub 공개 레포 + Claude Code 플러그인 매니페스트 | caveman과 동일한 설치 경험 |
| CI | GitHub Actions | 규칙 테스트가 항상 돌아야 문서의 수치가 썩지 않음 |

## 활용 스킬

- **`skill-creator`** — SKILL.md 작성 규약(프론트매터, description 트리거 문구, 간결성) 확인. 이 프로젝트의 산출물 자체가 스킬이라 직접적임.
- **`code-review`** — 커밋 전 파이썬 패키지 리뷰(보안 체크리스트 반영 여부 포함).
- **`verify` / 실제 실행** — `python -m gaejosik bench`와 `pytest`를 실제로 돌려 수치를 만들어냄. 이 프로젝트에서 "검증 없는 커밋"은 곧 제품 실패임.
- **`caveman` 스킬 원문** — 참조 설계(강도 레벨, 지속성, Auto-Clarity 예외 처리 구조)를 벤치마크.

## MVP 범위

**IN (오늘 만듦)**

1. `skills/gaejosik/SKILL.md` — 한국어 개조식 출력 스타일. 강도 3단(`lite`/`full`/`ultra`), 지속성 규칙, 압축 해제 예외(보안 경고·되돌릴 수 없는 작업·순서가 중요한 절차), **측정으로 기각된 안티규칙 목록**.
2. `gaejosik` 파이썬 패키지 — 측정된 규칙 코퍼스 1개(단일 진실 원천) + `bench`/`count` CLI.
3. `pytest` 회귀 테스트 — 각 규칙이 실제로 토큰을 줄이는지, 안티규칙이 정말 이득 없는지, 스킬이 내건 절감률 주장이 여전히 참인지 검증.
4. Claude Code 플러그인 매니페스트(`.claude-plugin/`) — 마켓플레이스 설치 가능.
5. 한국어 `README.md` — 실측 표(CLI가 생성), 설치법, 프록시 토크나이저 한계 명시.
6. GitHub Actions CI.

**OUT (로드맵으로)**

- 실제 파일 후처리 압축(caveman-compress 대응) — 출력 토큰 절감과 무관하고 별도 LLM 호출 비용 발생
- 일본어/중국어 확장
- 웹 데모 / 사용량 대시보드 / 팀 리포트
- PyPI 배포, VS Code 확장
- 세션 실사용량 자동 집계 훅

## 로드맵

1. **실사용 검증**: 본인 세션에 켜고 2주간 `/context` 사용량 비교. 실제 절감률이 벤치와 얼마나 다른지 기록(벤치는 통제된 문장, 실사용은 코드블록 비중이 커서 절감률이 낮게 나올 것 — 정직하게 보고).
2. **배포·채택**: GitHub 공개, 한국 개발자 커뮤니티(GeekNews, 모두의AI 마켓플레이스) 공유. Anthropic API 키 사용자로부터 실제 Claude 토크나이저 수치 수집(PR로 표 갱신).
3. **수익화 착수**: 스타 500/주간 설치 100 도달 시 팀 티어(조직 규칙 커스터마이즈 + 절감 리포트). 동시에 일본어판으로 시장 2배.
